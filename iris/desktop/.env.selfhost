# Self-host (open-source) build — runs workflows fully locally (BYOK).
# No cloud login, no cloud profile, no cloud connection polling.
# Activated by `vite --mode selfhost` (see package.json `*:selfhost` scripts).
VITE_SELF_HOST=1
